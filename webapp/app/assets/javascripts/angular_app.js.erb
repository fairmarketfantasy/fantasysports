angular.module('app', [
      'app.services',
      'app.data',
      'app.directives',
      'app.filters',
      'app.controllers',
      'app.templates',
      'angular-flash.service',
      'angular-flash.flash-alert-directive',
      'ui.bootstrap'])
.config(['$routeProvider', '$locationProvider', '$httpProvider', function($routeProvider, $locationProvider, $httpProvider) {
    $routeProvider.
      when('/', {templateUrl: '/home.html', controller: 'HomeController'}).
      when('/market/:market_id/roster/:roster_id', {templateUrl: '/roster.html', controller: 'RosterController'}).
      when('/market/:market_id/roster/:roster_id/vs/:opponent_roster_id', {templateUrl: '/roster.html', controller: 'RosterController'}).
      when('/market/:market_id', {templateUrl: '/home.html', controller: 'HomeController'}).
      when('/rosters', {templateUrl: '/roster_list.html', controller: 'RosterListController'}).
      when('/history', {templateUrl: '/roster_list.html', controller: 'RosterHistoryController'}).
      when('/leaderboard', {templateUrl: '/leaderboard.html', controller: 'LeaderboardController'}).
      when('/rules', {templateUrl: '/rules.html', controller: 'RulesController'}).
      when('/terms', {templateUrl: '/terms.html', controller: 'RulesController'}).
      when('/promos', {templateUrl: '/promos.html', controller: 'PromosController'}).
      when('/legal', {templateUrl: '/legal.html', controller: 'LegalController'}).
      when('/support', {templateUrl: '/support.html', controller: 'SupportController'}).
    //...
      otherwise({redirectTo: '/'});

    $httpProvider.defaults.useXDomain = true;
    delete $httpProvider.defaults.headers.common['X-Requested-With'];
  }]);
