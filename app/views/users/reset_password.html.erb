<% content_for :javascript do %>
  <script type='text/javascript'>
  $(function(){
    angular.element('body').scope().$apply(function() {
      var token = '<%= resource.reset_password_token %>';
      angular.element('body').scope().resetPasswordModal(token);
    })
  });

  </script>
<% end %>